{
  "title": "Device Registration Request Schema",
  "type": "object",
  "properties": {
    "auth_code": {
      "type": "string",
      "description": "auth code for device token request"
    },
    "client_id": {
      "type": "string",
      "description": "client ID for device app"
    },
    "client_secret": {
      "type": "string",
      "description": "client secret for device app"
    }
  }
}